const detectObjects = async (imageBase64, mimeType) => {
  // Backend offline fallback stub for COCO-SSD.
  // Real browser offline detection is implemented in the frontend.
  return [];
};

module.exports = {
  detectObjects
};
